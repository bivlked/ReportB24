# ==================== TESTING DEPENDENCIES ====================
# Фреймворки и инструменты для тестирования
# Версии актуальны по состоянию на 2025-10-26

# Основные production зависимости
-r requirements.txt

# ==================== TESTING FRAMEWORK ====================

# Pytest - основной фреймворк для тестирования
pytest>=8.4.1

# Pytest-cov - измерение покрытия кода тестами
pytest-cov>=6.0.0

# Pytest-qt - тестирование Qt приложений (если используется GUI)
pytest-qt>=4.4.0

# Pytest-xdist - параллельное выполнение тестов
pytest-xdist>=3.6.0

# ==================== MOCKING AND TESTING UTILITIES ====================

# Responses - мокирование HTTP запросов
responses>=0.25.0

# Freezegun - мокирование дат и времени
freezegun>=1.5.0

# ==================== ПРИМЕЧАНИЯ ====================
# Установка: pip install -r requirements-test.txt
# Это установит все необходимое для тестирования, включая production зависимости
#
# Запуск тестов:
#   pytest                     # запуск всех тестов
#   pytest --cov              # с измерением покрытия
#   pytest -n auto             # параллельное выполнение
#   pytest --cov --cov-report=html  # HTML отчет покрытия
